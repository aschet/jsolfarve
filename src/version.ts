// SPDX-FileCopyrightText: 2026 Thomas Ascher <thomas.ascher@gmx.at>
//
// SPDX-License-Identifier: MIT

/**
 * The package version.
 *
 * Kept in sync with `package.json` by a unit test rather than read at runtime,
 * so the bundled output never has to resolve a file relative to itself.
 */
export const VERSION = '1.0.0';
